### 正しく動かない理由
ボタンを押下してから値が更新されるまでにラグがあるので、その間に、ボタンを連打されると、それによる更新処理は、以前として更新前のデータを元にインクリメントされるため、連打した分値が増える動きにはならない。

setLikesでは、更新する値を設定するのではなく、前のstateに+1する更新関数を指定する。こうすることで、イベントの処理が、キューに登録されて、順番に処理され、更新する値はstateで管理してる値となるので、連打してもその分だけ更新される。
