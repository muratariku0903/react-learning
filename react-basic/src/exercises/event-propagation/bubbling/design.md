# 設計メモ

## 実装方針

<!-- どの要素で、どのようにイベント伝播を制御するか記述してください -->
削除ボタンを押下した際のイベントを親要素のカードに伝播しように、クリックイベントにstopPropagationを適応する。

---

### 評価コメント

設計意図と実装が一致している。`e.stopPropagation()` を削除ボタンの `onClick` 内で呼び出すことで、親のカード選択イベントへの伝播を正しく防止できている。state配置も適切。問題なし。
