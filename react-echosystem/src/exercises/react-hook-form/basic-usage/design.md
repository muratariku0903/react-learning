useForm の設定

mode は何にする？ なぜ？（Q3 の議論を踏まえて）
ModeはonChangeにします。入力されたタイミングでisValidかどうかの判定をしたいからです。
defaultValues をどう渡す？（Q3 の isDirty の落とし穴を踏まえて）
デフォルト値は文字列だったら空文字、数字だったら0を指定しておきます。
各フィールドの登録方法

name / email / age / bio → register or Controller? なぜ？
レジスタでいきます。レフを渡すことができるのと、あとは入力したタイミングで再レンダリングといった副作用が発生しないためです。
rating → register or Controller? なぜ？（Q2 の議論を踏まえて）
バリデーションの書き方
こちらはrefを注入することが難しいので、コンポーネントでコントローラーで制御することにします。
rulesを指定してバリデーションを実施します

register の第2引数に渡すか、useForm({ resolver }) を使うか
今回はどちらを選ぶか、その理由
今回、第2引数に直接渡します。そこまで重たいバリデーションではないので、シンプルなものなので直接書いた方がいいかなと思いました。

submit ボタンの活性条件
ボタンにdisabled属性があるので、disabledになるときの条件を考えて、それは編集されていない状態、あるいはバリデーションで不正な状態のときにdisabledにします。逆に言うと、それ以外の状態は活性にします。

isDirty && isValid の式に至るまでに、Q3 の落とし穴をどうクリアするか
エラー表示の方針
判定タイミングを onchange にします

各フィールドの下に出すとして、errors.xxx?.message のアクセスで型安全になるためにどう書くか
