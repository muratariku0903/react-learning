1. **Network タブ**: 最初のHTMLレスポンスにデータが含まれているか？
  * SSG、SSRの場合は最初のHTMLレスポンスにデータが含まれていました。しかしCSRの場合は最初のHTMLレスポンスにはデータが含まれていませんでした。CSRの場合はレスポンスは空のデータでしたね。空のデータ、クエリストリングパラメーターがRSCとなっていて、そのデータが空のデータとなっていました。
2. **View Source**（右クリック → ページのソースを表示）: HTMLにデータが埋め込まれているか？
  * SSCとSSRはHTMLの中にデータが埋め込まれていましたが、CSRの場合は埋め込まれていませんでした。
3. **ページ遷移時**: `<Link>` で遷移した場合と、ブラウザでURL直打ちした場合の挙動の違い
  * 本番モードで動作確認をしています。next buildをした後、next startコマンドを実行しました。SSGの場合は、ネットワークタブを見ているんですけど、リンクを遷移した場合とURLを直打ちした場合で、特に何もリクエストは飛んでいないですね。SSRの場合は、リンクから遷移した場合は、ネットワークタブの場合はリクエストが一つ呼び出されていまして、これがおそらくRSCペイロードがレスポンスとして返ってきているものになって、サーバーコンポーネント内で呼び出しているフェッチに該当するのかなと思います。SSRで直打ちした場合は、ネットワークタブを見ると何も確認できませんでした。CSRはどちらもPOSTというフェッチリクエストが実行されているのみですね。直打ちでもリンクから遷移した場合でも同様にPOSTというリクエストが実行されていました。
  * 上記の観察は不十分でした。まずSSGについてですが、SSGの場合はリンクから遷移した場合、ネットワークタブを確認するとコンテンツをネットから取得しているようには見えません。確認できませんでした。SSGのURL直打ちでは、ドキュメント、CSS、フォント、JSなどのソースが読み込まれています。ドキュメントの中にはHTMLが含まれており、ポストの一覧の内容が組み込まれています。

続いてSSRですが、SSRの場合、リンクで遷移した場合、FetchRequestが実行されており、レスポンスの内容は200で返ってきていますが、その内容は確認できませんでした。ドキュメント、CSS、JS、フォントなどはロードされていません。SSRの直打ちでは、ドキュメントにHTMLとPOSTのデータが組み込まれたHTMLとCSフォントがロードされています。

続いてクライアントコンポーネントですが、リンクの場合、POSTというリクエストが実行され、JSONのデータが返却されています。ドキュメントについては、CSS.jsは他と同様にデータの取得はされていません。

続いてCSRの自分の形ですが、こちらではまずフェッチリクエストが実行され、リンクのときと同じくJSON形式でデータが取得されています。ドキュメントに関しては、HTMLが開始されており、POSTの投稿データなどは含まれていません。シェルのみが返ってきています。CSS.jsは他と同様にロードされています。
4. **`next build` してから `next start`** で確認すると、SSG と SSR の違いがより明確に見える（dev モードではどちらも似た挙動になる点に注意）
  * 開発モードだとキャッシュが効かない状態になっているので、本番モードで動作確認をした結果、SSGの場合は即座にページが表示されていました。ネットワークタブを確認してもリクエストをしている痕跡が見当たりませんでした。SSRの場合はSSGほど早く表示されませんでした。ネットワークタブを確認するとリクエストしている痕跡が見当たりました。

---

### 実装レビュー

#### 第1回レビュー
- ~~`return`抜け~~ → 修正済み ✓
- ~~`loading`初期値~~ → `true`に修正済み ✓
- ~~`cache: "force-cache"`~~ → 削除済み ✓
- ~~表示時刻のTODO~~ → 実装済み ✓
- ~~ページ遷移の観察~~ → 再観察済み ✓

#### 第2回レビュー（最終）

**コード** — 3件のバグ修正すべて正しく対応できています。

**観察結果について補足：**

再観察の内容は大きく改善されています。核心的な違いを整理すると：

| | `<Link>`遷移 | URL直打ち |
|---|---|---|
| SSG | リクエストが見えない（後述） | フルHTML + CSS/JS/フォント |
| SSR | RSCペイロード（Fetchリクエスト） | フルHTML + CSS/JS/フォント |
| CSR | JSONデータ取得のみ | フルHTML（シェルのみ）+ JSONデータ取得 |

**SSGの`<Link>`遷移でリクエストが見えない理由：**
Next.jsの`<Link>`コンポーネントは本番モードで**プリフェッチ**を行います。ビューポートに`<Link>`が表示された時点で、バックグラウンドでRSCペイロードを事前取得しています。そのため、クリック時には既にキャッシュされたデータが使われ、新たなリクエストが発生しないように見えるのです。トップページが表示された時点のNetworkタブを注意深く見ると、SSGページ用のプリフェッチリクエストが確認できるはずです。

**実装演習 完了** — 演習4-1の言語化演習・実装演習ともに合格です。
